---

# Whether to install podman in rootless mode
podman_rootless_mode: true

# Install podman compose plugin
podman_compose_install: true

# Install and configure buildah as podman builder
podman_buildah_install: false

# Install img as podman builder
podman_img_install: true
